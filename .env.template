# Axolync notify defaults (agent-owned single source of truth).
# Copy to .env at a workspace or repo root to override; the nearest ancestor
# .env wins. This template file itself is never edited per-machine.
AXOLYNC_NOTIFY_BASE_URL=https://ntfy.sh
AXOLYNC_NOTIFY_CHANNEL=miki800_done
AXOLYNC_NOTIFY_BOT_NAME=Codex
