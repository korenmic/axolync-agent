interface:
  display_name: "Axolync Build Mirror Handoff"
  short_description: "Build and mirror Axolync artifacts from primary-workspace local checkouts only"
  default_prompt: "Use $axolync-build-mirror-handoff to fetch the requested branches under the current primary workspace root, run the requested build/report mode, and mirror artifacts to E:."
