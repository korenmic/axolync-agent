---
name: s2s
description: "Alias for seed-to-spec. Use when the user invokes $s2s or s2s to convert one seed or a group of seeds into requirements.md, design.md, and tasks.md with open-question handling first."
---

# S2S

Use the `seed-to-spec` skill. This skill is only a reliable alias so `$s2s` triggers the seed-to-spec workflow without duplicating instructions.
